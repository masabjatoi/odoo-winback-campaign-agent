version: '3.8'

services:
  win-back-cron:
    build: .
    container_name: win-back-agent-cron
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      # Mount persistent volume for the SQLite database
      - winback-db-volume:/app/data
    dns:
      - 1.1.1.1
      - 8.8.8.8

volumes:
  winback-db-volume:

